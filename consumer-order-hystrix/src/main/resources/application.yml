server:
  port: 8900
spring:
  application:
    name: consumer-order-hystrix
user:
  url: http://localhost:7900/user/
eureka:
  client:
    service-url:
      defaultZone: http://user:123@localhost:20000/eureka

  instance:
    prefer-ip-address: true
logging:
  level:
    com.qianfeng.springcloud.feign.FeignClient02: debug
